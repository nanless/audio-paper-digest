# Reader 清晰度与 Token 效率改进计划

状态：工程实现与完整回归已完成；真实试写仍有事实阻断，未发布、未宣布质量目标达成。继续执行用户追加的全量源文重写及事实验收。基线：`e39fff3`。

## 不变的边界

- 保留 canonical 的 13 个解析标题、Reader 12–18 节、5000–18000 中文字、4–10 组术语桥。
- 保留全文来源、数字/单位、公式、图片像素、作者机构与资源身份门禁。
- 保留论文锁、原子写、generation、review 只读、精确 Git delta 和远端 OID 验证。
- 不恢复本机助手，不运行整批日更，不自动重写或发布已有博客。
- 所有项目脚本和测试在沙箱外运行；不绕过环境守卫。

## 本轮实施包

| 工作包 | 负责人 | 交付 | 必须验证 |
| --- | --- | --- | --- |
| A. Reader 局部修复与恢复 | Reader Agent | 受限块补丁、输入 SHA 校验、失败候选持久化、无进展检测、相关证据上下文 | 多错定位；未改节点不变；拒绝越权/陈旧补丁；失败恢复不当作成功 |
| B. 发布前置检查与缓存 | Publication Agent | 确定性/Hugo 先于 LLM；协议匹配缓存；结构化阻断记录 | 已知机械错误调用 LLM 为零；模型/Prompt 漂移不复用旧通过；字节与事务回归 |
| C. 统一验证与 CI | Verification Agent | 完整验证入口、固定 Hugo 与必跑集成检查、离线回放框架、分层验证说明 | 全套 JS、默认/Manual Python、语法、数据、Hugo；不能静默 skip 关键集成 |
| D. 用量与成本归因 | 主 Agent | 请求级真实 usage/不可得状态、论文/阶段上下文、成本汇总命令 | Responses/Chat/Anthropic usage；失败计数；不记录正文/密钥；估算不混入实际值 |
| E. Reader 契约与确定性表格 | 集成阶段 | 共享机械契约、原表选择后由代码渲染、实际章节文风检查、精简写作规则 | Prompt/代码一致；表格位置与数字关联；Reader 跨节近重复；最终旧协议兼容 |
| F. 验证闭环与交付 | 主 Agent + 独立复核 | 基线回放、故障注入、完整回归、一篇受控真实验证、结果报告 | 有失败即定位修复并重跑；不宣称未实测的节省比例 |

模块所有权按文件/函数区段分开；并行修改不得覆盖其他 Agent 的变更。公共配置与包命令由主 Agent 协调。

## 详细实现顺序

1. 建立基线：记录工作树、现有成功产物 SHA；不对 canonical 或线上页面做实验性写入。
2. A/B/C 并行，主 Agent 实现 D；各工作包先完成故障用例再运行针对性回归。
3. 集成 A/D/E：原表仅选择来源行列，代码生成表格及绑定；模型专注解释。局部补丁合并后仍完整解析、验源和验文风。
4. 保留现有硬约束但消除提示冲突；章节近重复检查以真实 Reader 章节为单位。通用规则与论文特定反馈分离。
5. 所有便宜确定性检查在付费审查前完成；审查缓存须绑定实际审查协议与输入，旧指纹不能冒充当前结果。
6. 离线回放至少覆盖：多个错误、错误数字/单位、图 marker 错位、坏 JSON、陈旧补丁、候选损坏、恢复、无进展、协议漂移、Hugo 失败、真实跨节重复。
7. 运行完整验证。任何失败均修复后重新运行受影响套件，再运行完整入口。
8. 真实验证先限制为一篇已归档同源论文的隔离 Reader 实验；显式控制请求次数和输出预算，使用项目公共请求路由。失败保留记录，不无限追加调用，不覆盖已发布 canonical。
9. 独立复核结果，记录实际 usage、修复范围、内容质量与剩余限制，再交付。

## 指标与验收

- 机械错误场景：LLM 调用数为 0；本地排版/表格组装不得改变来源事实。
- 补丁场景：只修改允许节点，未触及节点 SHA 全部不变；合并后全门禁通过才可成功。
- 恢复场景：相同输入可续失败候选；来源、模型、Prompt 或契约变化时拒绝错误复用。
- 缓存场景：审查要求/模型变更必须触发相关重审；不能只凭页面 SHA 复用旧协议。
- 统计场景：服务未给 usage 时标记不可得，不以字符估算冒充计费量；记录失败调用及完整生命周期。
- 内容场景：学习依赖、执行链、实验条件、指标方向和证据边界正确；字数/节数达标不是质量充分条件。
- 目标值（需后续固定评测集验证）：累计 Token 降低 30%、首轮机械通过率 85%、p95 局部修复不超过两轮。单篇实验不能证明这些总体目标达成。

## 明确不做的大迁移

本轮不废除 13 标题 canonical，不降低字数/章节要求，不替换模型，不取消事实审查，不把全部成功缓存直接改成宽松复用。
结构化 canonical 全量迁移、评分分歧维度裁决和大规模模型 A/B 留作独立迁移；先以本轮可回归的局部机制建立可靠基础。

## 用户追加的后续任务：9 月 4 日完全重写

此项仅在上述改进完成复验后执行，是用户后续明确授权，不改变前述隔离实验的边界。

- 范围：2026-09-04 的全部单篇博客及当日汇总页。
- 必须从原论文来源全新生成；旧 canonical 分析、旧 Reader、旧博客正文、旧修订反馈和旧生成候选不得成为新稿的模型输入。
- 原论文题目、作者等元数据、验证过的原始全文、工件和原图不是旧生成正文，可以继续使用；所有新结论仍须回到原文。
- 先保留可恢复备份与完整旧新 SHA 清单，核对全量论文集合，不从历史日期 fetch 重跑。
- 使用论文锁、全新生成身份与严格恢复边界；新运行内允许恢复本轮新生成的候选，不恢复旧稿。
- 完成全部新稿后按 generate → review → push 验证，检查所有目标页覆盖、来源和远端证明；发布后重新核对视觉要求或有效 waiver，不能复用绑定旧正文的过期证明。
- 实际执行状态见本文末尾；旧版正文保持隔离，只有新 run 内的稿件可以继续修复。

## 验证记录

运行日志及真实论文实验留在忽略目录，不提交密钥、数据或缓存。

### 第一轮集成与交叉复核

- `npm run verify` 完整通过：974 项 JS、387 项默认 Python、24 项 Manual Python；固定 Hugo 0.160.1、JS/Python/shell 语法、严格只读数据验证全部通过，JS 无 skip。
- 故障注入覆盖：正文失败局部恢复、陈旧/越权补丁、网络失败预算、协议漂移、审查块失败续跑、图片/上下文变更、Hugo 失败与危险缓存路径。
- 交叉复核发现并修正：网络失败错误消耗内容预算；空 usage / 非数组 choices 导致请求漏记；普通脚本导入 unittest.mock 误关闭用量记录；新增脚本缺索引导致全套 JS 失败。修正后针对性与完整套件复验通过。
- 源表真实回放发现：2609.03622 的结果表包含 all-header、空表头及 MathML/TeX 双写，无法安全走逐字选择。新增付费前 eligibility 提示，保留 quote 路线，不改源工件或放宽数值验证。

### 隔离真实实验 v1：保留失败，不当作完成

同源论文 `2609.03622`；完整来源 SHA `28ee1ee1b24f8534646a96d4f758e072379b836332d51c76ca1e14f1202a44c2`，
结构化工件 SHA `70341920846648bf553f6feb0503f1b8d90ec451a5ef02720ff3994376a9b180`。
模型保留项目当前 `muse-spark-1.3-contributor`，未改模型配置。先完成离线回放（0 次模型调用）。

v1 调用了 1 次完整生成和 2 次局部补丁，达到隔离预算后失败停止。provider 实报累计输入 58756、输出 16820、总计 75576 tokens；
其中两次补丁共输出 6541，低于首稿输出 10279，但**未产出验收成功正文，因此不能据此宣布端到端节省或质量提升**。
诊断发现模型混用 quote 表格与 selection marker，并发明 cellBindings 字段；每轮只暴露一个绑定错误，局部修复提示缺少明确 schema。
这不是靠增加重试次数解决的问题：下一轮明确三种表格输入模式，并一次汇总所有可独立判定的结构错误。

报告：`data/runtime/reader-efficiency-evaluations/2609.03622-live-v1/report.json`；原响应、失败候选与用量均保留。
来源、签名快照、`papers.json` 与 `deep-analysis-result.json` 前后 SHA 一致；未发布失败稿。
独立事实复核另发现失败稿将 DNS 的 WER 下降写成恶化，说明结构与数值绑定通过也不能替代语义/图像审查。

### 失败驱动的第二轮修正与完整复验

- 主生成与补丁 Prompt 均明确 selection / artifact_table / source_quotes 的互斥正文与绑定格式。
- 用真实失败样本一次发现 11 条独立问题；白名单含 14 个正文与 2 个表格绑定节点，父子重复为 0。篇幅只作提前提示，最终值仍由生产 parser 计算。
- 成功后将精确匹配的旧失败候选可恢复退役，不复活旧失败稿；正文次数预算及修复 Prompt/代码/输出预算均进入指纹。
- 补充 Hugo 失败入口回归：从 `review_blog.main()` 验证 LLM、receipt 和 push 调用均为 0。
- 第二轮 `npm run verify` 完整通过：980 项 JS（0 fail / 0 skip）、388 项默认 Python、24 项 Manual Python，以及固定 Hugo、全仓语法、严格只读数据验证。
- 日志：`/private/tmp/reader-efficiency-verify-v2.log`。第二轮隔离真实复验使用新鲜 `2609.03622-live-v2` 目录，仍为每轮最多 3 次请求，不复用 v1 失败稿。

### v2 实测：机械通过，不等于事实通过

v2 用 1 次完整生成通过生产 parser：13 节、7099 汉字、2 表、4 公式、3 图；provider 实报输入 25523、输出 9140、总计 34663 tokens。
相比 v1 的 3 次失败调用，这次机械闭环没有返工，但两轮均不是可直接发布的新版本，不能拿这个差额宣称总体质量或成本目标达成。

独立复核三张原图与全文后拒绝该稿：DNS WER 方向解释反转；把数据集密度分布与单例竖线混淆；将末步结果外推全程；
遗漏其他三集的可运行预测停止结果及逻辑回归训练/应用条件；将更高干净密度推成更强判别；将停止梯度写成消除二阶梯度。
部分错误与输入 canonical 的未证实解释高度重合。原始展示公式未被篡改，但这不能证明周围讲解正确。

因此继续做一项范围明确的输入修正：Reader 默认直接依据原文证据与原图，不再重复输入生成的 canonical 评论；
13 标题 canonical 仍保留给机器字段、评分及其他消费者。保留显式修订模式，但正常 source-only 生成不得借入旧稿。
同时减少诱导无来源反事实解释的要求，明确图例对象、时间范围和实际可运行策略的比较。下一次限额实验不注入本论文专属纠错答案。

### v3 与本轮工程最终验收

v3 为 `reader-source-only-v1`：1 次完整生成 + 1 次局部补丁后通过生产 parser，13 节、6646 汉字、2 表、4 公式、3 图。
原文和 canonical 前后 SHA 未变。首稿实报输入 20613 / 输出 8313，补丁输入 22279 / 输出 5726，总计 56931 tokens。
首稿输入比 v2 减少约 19.2%，但补丁输入比首稿还大、端到端总量比 v2 高；**不能将局部输出更少或输入隔离等同于总体节省**。

独立事实审查仍为 FAIL：WER 方向反转、把昂贵写成不可部署、波形参考与转录参考混淆、无来源的因果推断、主结果数字缺失。
较 v2 修正了 Figure 1 分布身份、二阶梯度说法与同分布即泄漏的错误，但“够两张宽表”未能保证主结果覆盖。
所有实验稿均留在隔离目录；旧博客、canonical、评分均未被这些试稿替换。后续整批重写必须继续逐篇事实和图像审查，不能直接发布 parser pass 稿。

最后另用红绿测试修正了明确返回截断/不完整响应的跨次预算漏洞：`MODEL_OUTPUT_TRUNCATED` / `MODEL_OUTPUT_INCOMPLETE`
消耗内容预算，完整输出同时消耗完整生成次数；保留完整旧候选、拒绝半截 JSON，真正网络故障仍可恢复。
最终 `npm run verify`：985 项 JS（0 fail / 0 skip）、388 项默认 Python、24 项 Manual Python；固定 Hugo、全部语法和只读数据均通过。
完整日志：`/private/tmp/reader-efficiency-verify-final.log`。

本轮可交付的是经过回归的输入隔离、局部修复/恢复、前置审查、精确缓存、实际用量和统一验证机制。
尚未证明的是文章事实质量普遍提升、总体 Token 降低 30% 或首轮通过率 85%；三次同一论文实验不能替代固定评测集。

## 9 月 4 日实际重写运行

- 工程改进本地恢复点：`51a6b72`（未推送代码或实验文章）。
- 新运行：`dd07d37b-74fd-4d50-92f2-93e9c8f6f752`，目录 `data/runtime/fresh-rewrites/<runId>/`。
- 旧版基线：博客 `469f2a936bfcec6da42a6708573b242a8c2e5982`；canonical generation `830`。
- 实际备份 107 份、168166008 bytes，覆盖 31 页及图片/引用侧车、整批和单篇凭证与核心数据。独立重放 SHA 和 0600 权限通过。
- 30 篇 inputs 与 raw 原始元数据白名单逐值一致；initial analysis 没有旧生成字段。旧生成正文只在恢复备份中。
- 30/30 原始全文与完整工件已验源缓存。03321 的差异只来自 `sourceId` 是否带 `v1`，使用原始版本的官方请求后通过原有 SHA；未更改基线或期望。后续新 prepare 已保留原始版本 ID。
- 新增最低主结果覆盖检查：原文明确报告定量结果时，不能仅靠数据/配置表满足表数；真实 v3 失败样本已离线重放并被生产 parser 拒绝（0 模型调用）。该检查仍不替代语义事实审查。
- 原子写更新现保留私有文件原权限，避免 run/checkpoint 从 0600 退回 0644。
- 重写用量可按 `npm run usage:report -- --run dd07d37b-74fd-4d50-92f2-93e9c8f6f752` 独立汇总；未知与真实 token 分开，修复尝试不通过新 runId 清零。
- 下一阶段：同 run 内 3 篇并行全新分析 → 独立正文/图像核查及必要修复 → 完整批次提升 → generate/review/push → 发布后视觉和最终状态。

### 真实批次暴露的问题与暂停修正

首次运行在 Reader 环节反复失败后主动停止，保留 30/30 原文、已完成的 canonical/评分检查点及 Reader 内容预算；截至暂停，尚无完整成功论文，未提升或发布。被中断的请求可能没有完整 usage 回执，因此记录不是完整账单。

- 正文按 section kind 排序时，表格绑定未同步重排，导致错误信息与修复节点错位。统一规范器现在同步正文、绑定和 selection marker，保存原始/规范索引及双 SHA，歧义拒绝。
- 原始 TeX 颜色命令粘连数字导致有效证据未被识别；修复 Node/Python 两端后，交叉测试又发现命令外负号可能被丢失，补充保守拒绝和负值回归。原文、quote、单位门禁不变。
- 单位分列、千分位/精度改写及 quote 换行差异需要修改正文或引用本身，不能只追加含同值数字的引文。增加带真实正文路径、原表上下文及逐字原句的诊断，不把候选数字当成事实证明。
- 显式 `--refresh-reader-diagnostics` 只迁移同 run、同来源/模型/主 prompt/预算的失败候选；内容和完整生成次数不清零，旧无进展状态保留审计。新版本仍须免费重跑完整 parser，付费请求仍受原上限约束。
- 三个子代理已为全部 30 篇分别完成原文事实核查清单，后续新稿须逐篇对照，不把 parser pass 当作事实 pass。
- 交叉审查注入归档 I/O 故障，复现并修正 save-new/rename-old 中断重入缺口；归档绑定完整旧 payload/envelope SHA，并验证删改、symlink 和路径越界。全部真实失败候选只读规范化重放通过，未在测试阶段迁移生产候选。
- 最终统一回归日志：`/private/tmp/fresh-diagnostics-verify-final.log`，Hugo、JS/Python/Manual、全仓语法与严格只读数据验证全部通过；随后才显式恢复原 run。

### 同源新稿的局部纠错与真实文体故障

- 首轮恢复批次已跑完：30 篇均有本轮分析，20 篇 Reader 机械完成、10 篇仍失败，尚未提升或发布。逐篇事实审查与真实图像复核发现机械通过仍可能包含指标方向、样本归属和未测因果错误，保留具体新稿 SHA 后再修订。
- 新 `patch` 阶段只允许同 run 活动失败候选的已有节点变更；完整生产 parser 通过才保留为失败恢复输入，请求预算不变，原字节和补丁可恢复归档。`analyze --ids` 只复验已修篇，最终状态仍按全批统计，未选论文和预算不动。
- 真实样本证明文体自动改数存在破坏：0.55/0.45 被写成 0点5、48kHz被变为8040赫、转录小数丢位。新版保留明确引语/代码/公式，完整解析可确定的小数，歧义量级不猜改，不按数值大小补百分号或复制单位。
- 三分之一倍频程采用精确上下文豁免，并修正原始TeX注入后诊断索引漂移；文体诊断与豁免共同绑定注入前质量视图，源公式/数字门禁另行执行。
- 新稿残留术语marker的原因是错位marker被自动再插一次。现在已有marker必须唯一且位于声明小节，只有完全缺失时才保留原确定性补放行为。
- 本轮最终全套回归日志：`/private/tmp/fresh-pre-repair-verify-final.log`；任务专用signed修订调用另有12项零API故障注入，确保持久结果安装中断后免费重入，不因已完成快捷分支绕过父稿/完整paper SHA。

### 全量新稿后的事实复核与精简 Prompt

- 30 篇新 canonical 与 Reader 均已生成，仍在隔离 run 内。逐篇源文/原图复核发现数值可重放但指标列错配、原论文内部矛盾被擅自解释、未测量效果被承诺等问题；这些必须修正，不能由机械成功替代事实验收。
- 原始 10 篇失败候选采用已审局部补丁后由正常引擎免费完整复验。定向修订又有 4 篇在局部段落/数量写法上阻断，现允许同 run、有效同源签名父稿的 failed scratch 经原签名修订入口恢复；父稿/反馈身份不变、预算不重置。该 4 篇恢复前后用量记录没有增加请求。
- 签名失败候选及严格等价逆变换实现的完整验证：1090 项 JS、390 项默认 Python、24 项 Manual Python，以及 Hugo、全仓语法和严格数据检查；本地恢复点 `b2406db`，日志 `/private/tmp/fresh-signed-scratch-verify-final.log`。
- 写作 Prompt 合并重复原则，保留原有 JSON/schema、首个 fenced block、数量门槛、表格三模式和全部来源要求；明确无训练不等于确定性、参数状态必须有源、差值不能错列、单位与数值同格、原文矛盾要揭示。去掉固定表前/表后段落数量的额外写作负担，仍须满足生产 parser 的相邻段落及汉字要求。
- 完整 Prompt 文件从 9551 缩至 7244 字符（约 24.2%）；这是字符长度，不是 tokenizer 计数、计费量或端到端节省率。新 Prompt 只作用于后续新请求，不回写本批已生成文章的历史 Prompt SHA，也不为更新 Prompt 再生成 30 篇。运行时占位符、schema 和 source-only 回归 98 项通过，完整集成验证另行执行。数字带单位时不拆格；原文裸数值则不擅自补单位，避免新的示例冲突。
- 此轮方法参照 [OpenAI 的 Prompt engineering](https://developers.openai.com/api/docs/guides/prompt-engineering) 与 [Evaluation best practices](https://developers.openai.com/api/docs/guides/evaluation-best-practices)：提示明确、按真实任务建测试，并结合事实审查校准机械指标。未套用其他模型专有设置，当前模型和调用预算保持不变。

本批用量截至上述 4 篇恢复完成：296 条有 provider 回执的请求，输入 5690018、输出 1356645、总计 7046663 tokens，其中 8 条请求未成功。被早先中断而未落盘的调用可能缺少回执；此值不是完整账单，也不能证明总体 Token 节省。已确认的局部收益是这些确定性修复和重入没有再发模型请求；后续固定评测集仍须同时统计首次事实通过率、总请求和总用量。
