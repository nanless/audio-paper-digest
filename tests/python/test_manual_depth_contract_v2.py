import os
import sys
import unittest

ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), '..', '..'))
SCRIPTS = os.path.join(ROOT, 'scripts')
sys.path.insert(0, SCRIPTS)

from publish_common import (  # noqa: E402
    MANUAL_DEPTH_CONTRACT_VERSION,
    MANUAL_DEPTH_CONTRACT_VERSION_V2,
    MANUAL_DEPTH_CONTRACT_VERSION_V3,
    find_cross_section_duplicate_sentences,
    validate_manual_depth_contract,
    validate_manual_depth_contract_v2,
    validate_manual_depth_contract_v3,
)

METHOD_PARAGRAPH = (
    '输入端把音频波形转换为声学特征，并由编码器提取局部信息；中间模块负责融合上下文和长程依赖，'
    '输出端通过任务头生成识别结果。训练阶段使用公开数据和噪声增强构造多种条件，优化过程包含监督目标、'
    '验证集选择和推理时的解码设置。推理流程先完成特征抽取，再进行上下文融合，最后输出识别序列；'
    '各阶段的输入、输出和评价指标保持对应，消融结果只归因于实际移除的模块。'
)


def valid_analysis():
    method = '\n\n'.join([METHOD_PARAGRAPH] * 4)
    results = (
        '实验在多个带噪语音基准上比较主方法与基线，并报告识别错误率 8.6%、噪声鲁棒性和消融变化。'
        '结果同时记录数据划分、基线和评价口径，不能把单一条件的改善外推到所有说话人或设备。\n\n'
        '| 实验 | 结果与条件 |\n|---|---|\n| 主方法 | 多个公开基准、带噪条件 |\n| 消融 | 移除上下文模块后误差上升 |'
    )
    scoring = '\n'.join([
        '* 创新性 (1.5/2)：[A_METHOD] 方法有明确增量，可定位到上下文融合模块设计。',
        '* 技术严谨性 (1.2/1.5)：[A_METHOD] 核心假设和模块关系清楚。',
        '* 实验充分性 (1.1/1.5)：[A_RESULTS] 覆盖主要基准并提供消融实验。',
        '* 清晰度 (0.8/1)：[A_CLARITY] 结构描述清楚，模块关系直接。',
        '* 影响力 (1.0/1.5)：[A_IMPACT] 对语音识别读者有参考价值。',
        '* 开源 (0/1.5)：[A_OPEN] 未说明开源资源，可得性不能确认。',
        '* 可复现性 (0.3/0.5)：[A_REPRO] 部分超参数缺失但主体流水线完整。',
        '* 工程/实践价值 (1.0/1.5)：[A_ENGINEERING] 结构能接入常见语音识别系统。',
    ])
    return f'''## 评分
6.9/10

## 毒舌点评
工作的问题定义清楚，但方法增量和工程证据仍有提升空间。

## 核心摘要
论文围绕语音识别鲁棒性提出完整方法，在多个标准基准与噪声条件中验证性能，错误率与消融方向均有交代。

## 方法概述和架构
{method}

## 核心创新点
引入上下文融合模块，覆盖主要噪声条件，并保持常见语音识别架构兼容性。

## 实验结果
{results}

## 细节详述
训练细节包括数据处理、优化策略、模型训练和推理设置，并说明主要模块之间的数据流与验证集选择方式。

## 评分理由
{scoring}

## 局限与问题
论文对极端噪声、跨域数据、部署成本和失败案例的讨论仍不充分。

## 开源详情
未提及代码、模型或数据集开放地址。
'''


def valid_v3_analysis():
    summary = ''.join([
        '论文面对真实噪声条件下语音识别错误率上升的问题，先把评价对象限定为固定数据划分与公开基线，并说明现有系统在跨设备条件下的失效方式。',
        '作者提出由前端特征编码、上下文融合和任务解码组成的模型，使局部声学证据与长程语义约束在同一训练目标下协同更新。',
        '机制上的关键不是简单增加参数，而是用受控门控决定不同时间尺度的信息进入任务头的比例，从而减少瞬态噪声对输出序列的干扰。',
        '实验在三个公开基准、两个噪声区间和多组消融中与强基线比较，主方法的错误率从十二点四降至八点六，并在移除门控后出现明确回退。',
        '这些结果支持模块在论文覆盖条件中的有效性，不过跨语言、极低信噪比和长时在线部署仍没有得到同等强度的验证。',
        '因此文章的价值在于提供可核对的数据流与实验因果链，而不是把单一榜单提升外推成对所有语音场景都成立的普遍结论。',
        '读者由此可以区分被数据直接支持的性能改善、依赖固定配置的工程观察，以及仍需额外实验回答的泛化问题。',
    ])
    method_paragraphs = [
        '输入阶段接收十六千赫兹单通道波形，按固定窗口切成重叠帧并计算对数梅尔特征；训练集、验证集和测试集保持说话人互斥，归一化统计只从训练集估计，避免测试信息进入前处理。',
        '编码阶段由卷积下采样器提取局部模式，再把序列送入多层上下文网络；每个模块都保留残差路径，门控单元依据当前帧和历史状态生成融合权重，使短时细节与长程依赖沿并行分支汇合。',
        '训练阶段联合优化识别损失、门控稀疏约束和辅助一致性目标；优化器、学习率、批量大小、训练轮数与验证集早停规则均固定，消融只移除一个组件，其余配置和随机种子保持一致。',
        '求解阶段先完成前向编码，再通过束搜索解码候选序列；阈值、搜索宽度和语言模型权重由验证集选择，测试时不再调整，输出同时保留最终文本与逐帧置信度供错误分析。',
        '评价阶段把主方法、公开基线和移除门控的变体放在相同数据划分中比较，报告错误率、延迟与资源占用；这一流程明确了输入、组件、训练目标和输出之间的因果边界。',
        '增强阶段从训练语句生成混响、带宽受限和非平稳噪声版本，各版本仍沿用原始转录标签；采样概率在训练前固定，验证与测试语音不经过增强，从而把鲁棒性收益与测试污染区分开。',
        '状态更新阶段把当前编码向量、前一时刻隐藏状态和门控系数共同送入融合单元；局部分支保留辅音瞬态，长程分支追踪跨词依赖，最终投影层只接收融合后的统一表示。',
        '控制实验阶段分别固定参数量、训练步数和增强样本数，再替换融合规则或关闭辅助损失；这种逐项改变避免把训练预算差异误写为模块贡献，并为每个结果表建立直接的配置对应。',
        '复现阶段保存最佳验证轮次、解码参数和数据清单，推理脚本先校验输入采样率再输出文本；未披露的软件依赖和驱动版本明确记作缺口，不用默认环境替作者补齐。',
    ]
    innovations = '\n\n'.join([
        '既有方法通常把局部卷积和长程编码串联后统一更新，瞬态噪声会沿隐藏状态持续传播。本文引入按帧门控机制，把两类证据作为并行输入后再融合；消融中移除门控使错误率回升，说明收益并非只来自更大的参数量，但证据仍限于论文覆盖的噪声条件。',
        '传统训练只优化最终转录目标，难以判断中间状态是否稳定。作者增加辅助一致性约束，使同一句话的不同增强版本在表示空间靠近；实验报告跨设备条件下的错误率下降，并通过对照训练验证约束的贡献，不过没有证明该策略适用于未见语言。',
        '标准评测往往只报告单一平均分，掩盖部署代价。本文同时给出准确率、延迟和资源占用，并把失败样例按噪声类型拆分；这些数据让方法收益与工程代价可以共同核对，但尚未包含长期在线运行和真实用户研究。',
        '过去的系统比较还容易混用不同解码预算，使更宽的搜索被误认为模型本身更强。作者固定束宽、语言模型权重与测试硬件，再报告配对差异；该设计让实验归因更可信，不过证据没有覆盖服务器并发和移动端能耗，仍不能推出所有部署条件下都占优。',
    ])
    results = '''实验使用三个公开语音基准，并在相同训练集、验证集和测试集划分下比较主方法、标准基线以及两个消融变体。主方法的词错误率由基线的 12.4% 降至 8.6%，移除门控后回升到 10.9%，去掉一致性损失后为 9.8%；这些配对结果把性能变化与具体组件对应起来，而不是比较不同数据规模的系统。

| 条件 | 词错误率 | 延迟 |
|---|---:|---:|
| 标准基线 | 12.4% | 31 ms |
| 主方法 | 8.6% | 34 ms |
| 无门控 | 10.9% | 33 ms |

分噪声结果显示中等信噪比改善最稳定，但是极低信噪比下优势仅为零点几个百分点，跨设备集合也仍落后于同域测试。说话人分组还显示长尾群体的改善幅度小于总体均值，说明平均分会掩盖个体差异；延迟增加三毫秒虽未改变离线排序，却不能直接代表流式系统的端到端体验。因而结果只能支持固定语料和离线解码设置，尚未报告在线吞吐、置信区间或更大规模的人类听测，不能把平均错误率下降外推到所有语言与设备。'''
    details = ''.join([
        '数据方面，训练、验证和测试按说话人互斥划分，增强只作用于训练集，公开语料的版本号和采样率都保持固定。',
        '优化方面，模型使用自适应优化器与分段学习率，批量大小、训练轮数、早停耐心值和随机种子均在配置中给出，损失权重通过验证集确定。',
        '实现方面，输入窗口、步长、梅尔维度、隐藏层宽度、门控阈值和束搜索宽度都有明确取值，消融实验只改变目标组件。',
        '硬件方面，作者报告单张消费级 GPU 的训练环境和峰值显存，也说明 CPU 推理的线程数；这些信息足以估算资源但不足以复原全部系统软件版本。',
        '部署方面，测试时采用固定解码参数并记录平均延迟，尚未提供长时间流式吞吐、能耗和不同处理器的测量，因此工程结论仍应限定在给定设备。',
        '误差分析按噪声类别和说话人分组，避免只依赖总体均值；不过没有公开逐样本预测与完整训练日志，第三方复核仍需要额外工作。',
        '评测脚本先统一文本规范化、大小写和标点处理，再计算词错误率；延迟从特征输入开始计时并排除文件读取，资源占用使用同一批量和线程设置，避免把测量口径差异当成模型优势。',
        '检查点依据验证集错误率选择，所有消融沿用相同初始化和训练预算；论文仍未说明依赖包锁文件、驱动版本与确定性算子开关，因此精确复现最后一位小数并无保证。',
        '失败案例保留噪声类型、说话人组别和解码置信度，但不公开原始敏感音频；这种隐私边界合理，却也限制了外部研究者对个别错误的逐样本复核。',
    ])
    scoring = '\n'.join([
        '* 创新性 (1.5/2)：[A_METHOD] 按帧门控把局部与长程证据并行融合，消融回退支持机制贡献，但并非全新任务定义。',
        '* 技术严谨性 (1.2/1.5)：[A_METHOD] 损失、数据流和控制变量对应清楚，仍缺少对门控稳定性的形式证明。',
        '* 实验充分性 (1.1/1.5)：[A_RESULTS] 三个基准与两个消融覆盖主要主张，不过跨语言和在线场景证据不足。',
        '* 清晰度 (0.8/1)：[A_CLARITY] 图表、符号和模块关系组织明确，少量实现选择仍分散在附录。',
        '* 影响力 (1.0/1.5)：[A_IMPACT] 对噪声语音识别有直接参考价值，但现有证据尚不足以支持更广泛模态。',
        '* 开源 (0/1.5)：[A_OPEN] 正文没有给出本文代码、权重或新增数据的可访问地址，因此不能计开放分。',
        '* 可复现性 (0.3/0.5)：[A_REPRO] 数据划分、优化和关键超参数齐全，但软件版本与完整训练日志仍未说明。',
        '* 工程/实践价值 (1.0/1.5)：[A_ENGINEERING] 延迟与显存数据支持离线部署判断，尚缺在线吞吐和能耗测试。',
    ])
    limits = '''论文证据直接支持的边界：极低信噪比下的改进明显缩小，跨设备测试仍弱于同域集合；实验只覆盖三个公开语料和固定离线解码配置，没有报告置信区间、长期流式吞吐、能耗或多语言迁移。消融能够说明门控与一致性损失各自有贡献，但不能排除两者与数据增强之间的交互影响，也没有给出模块稳定性的形式分析。

进一步审视：即便平均词错误率下降，也不能推导所有说话人群体都同等受益；应补充分组误差、逐样本预测和多随机种子统计。部署结论目前来自单一硬件和短任务测量，缓存增长、温度降频与真实并发可能改变延迟排序。开源材料若缺少版本锁定、完整训练日志和评测脚本，第三方仍难以复核表中数字；后续还需要在未见语言、连续对话和真实用户条件下验证失败模式，而不是只扩充同分布测试集。'''
    method = '\n\n'.join(method_paragraphs)
    return f'''## 核心摘要
{summary}

## 方法概述和架构
{method}

## 核心创新点
{innovations}

## 实验结果
{results}

## 细节详述
{details}

## 评分理由
{scoring}

## 局限与问题
{limits}

## 毒舌点评
问题定义与证据链条清楚，但结果仍集中在同分布离线测试；在补齐跨语言、在线资源和统计不确定性前，它更像扎实的工程改进而不是普适结论。

## 开源详情
正文没有给出本文代码、权重或新增数据的可访问地址。
'''


class ManualDepthContractV2Test(unittest.TestCase):
    def test_versions_exposed(self):
        self.assertEqual(MANUAL_DEPTH_CONTRACT_VERSION, 'full-text-evidence-v1')
        self.assertEqual(MANUAL_DEPTH_CONTRACT_VERSION_V2, 'full-text-evidence-v2')
        self.assertEqual(MANUAL_DEPTH_CONTRACT_VERSION_V3, 'full-text-evidence-v3')

    def test_valid_analysis_passes_both_versions(self):
        analysis = valid_analysis()
        self.assertIsNone(validate_manual_depth_contract(analysis))
        self.assertIsNone(validate_manual_depth_contract_v2(analysis))

    def test_v2_rejects_cross_section_duplicates(self):
        duplicated = '这句话被人工复制到多个章节用于凑齐契约字数，属于典型的模板化素材复用行为。'
        analysis = valid_analysis()
        for title in ('核心摘要', '实验结果', '细节详述'):
            analysis = analysis.replace(f'## {title}\n', f'## {title}\n{duplicated}\n')
        self.assertIn('跨章节自我复制', validate_manual_depth_contract_v2(analysis))
        duplicates = find_cross_section_duplicate_sentences(analysis)
        self.assertEqual(len(duplicates), 1)
        self.assertEqual(len(duplicates[0]['sections']), 3)

    def test_v2_rejects_editorial_template(self):
        analysis = valid_analysis().replace(
            '工作的问题定义清楚，但方法增量和工程证据仍有提升空间。',
            '这项工作整体尚可。亮点是一是结构清晰，二是实验完整，三是结果稳定；短板是作者自己承认的不足。',
        )
        self.assertIn('固定模板句式', validate_manual_depth_contract_v2(analysis))

    def test_v2_rejects_missing_anchor_tags(self):
        import re
        analysis = re.sub(r'\[A_[A-Z_]+\]\s*', '', valid_analysis())
        self.assertIn('[A_*]', validate_manual_depth_contract_v2(analysis))

    def test_v1_still_accepts_text_without_anchor_tags(self):
        import re
        analysis = re.sub(r'\[A_[A-Z_]+\]\s*', '', valid_analysis())
        self.assertIsNone(validate_manual_depth_contract(analysis))

    def test_v3_requires_argument_flow_reproducibility_and_two_layer_limits(self):
        analysis = valid_v3_analysis()
        self.assertIsNone(validate_manual_depth_contract_v3(analysis))
        self.assertIn(
            '分开标注',
            validate_manual_depth_contract_v3(
                analysis.replace('进一步审视：', '后续问题：')
            ) or '',
        )


if __name__ == '__main__':
    unittest.main()
